#include<stdio.h>
struct student {
    char name[100];
    int roll_no;
    int marks[3];
int main()
    {
        int average;
        printf("Enter the number of students : ");
        for(i=0;i<n;i++)
        {
            struct student s[100];
        }
        s[i].roll_no = roll_no;
        s[i].name = name;
        printf("Enter name of the student : ");
        scanf("%d", &name);
        printf("Enter P, C and M marks");
        for(i=0;i<n;i++)
        {
            scanf("%d %d %d, s[i].Name, s[i].Marks[0], s[i].Marks[1], s[i].Marks[2]");
        }
        printf("\n");
        printf("--------------------------------------------------------------");
        printf("Name     Maths   Physics     Chemistry");
        printf("--------------------------------------------------------------");
        printf("%d",&n);
        printf("--------------------------------------------------------------");
        average = (s[i].Marks[0], s[i].Marks[1], s[i].Marks[2]/3)
        printf(%d",&average);
        printf("--------------------------------------------------------------");
    }
}